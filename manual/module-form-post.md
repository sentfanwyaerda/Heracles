HTML Form POST Authentication module
=======
It uses an HTML form to request authentication. Data to be send through ``$_POST``. The form could be provided by ``Heracles::authentication_form($username, $password)``.
